apiVersion: apps/v1
kind: Deployment
metadata:
  name: elasticsearch
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: elasticsearch
  template:
    metadata:
      labels:
        app: elasticsearch
    spec:
      securityContext:
        fsGroup: 1000  # Elasticsearch의 UID/GID에 맞는 값으로 설정
      volumes:
        - name: elasticsearch-storage
          persistentVolumeClaim:
            claimName: elasticsearch-pvc
      containers:
        - name: elasticsearch
          image: docker.elastic.co/elasticsearch/elasticsearch:7.14.1
          volumeMounts:
            - mountPath: /usr/share/elasticsearch/data
              name: elasticsearch-storage
          env:
            - name: discovery.type
              value: single-node
            - name: ES_JAVA_OPTS
              value: "-Xms512m -Xmx512m"
            - name: xpack.security.enabled
              value: "false"
          ports:
            - containerPort: 9200
              name: es-9200  # 포트 이름을 15자 이하로 수정
            - containerPort: 9300
              name: es-9300  # 포트 이름을 15자 이하로 수정
      nodeSelector:
        purpose: custom
---
apiVersion: v1
kind: Service
metadata:
  name: elasticsearch
  namespace: dev
spec:
  ports:
    - port: 9200
      targetPort: 9200
      name: es-9200  # 포트 이름을 15자 이하로 수정
    - port: 9300
      targetPort: 9300
      name: es-9300  # 포트 이름을 15자 이하로 수정
  selector:
    app: elasticsearch
