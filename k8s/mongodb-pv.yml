apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongodb-pv
  namespace: dev
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: standard
  hostPath:
    path: /mnt/data/mongodb  # 호스트 머신의 경로
